# b.spoke Spec Sites

Auto-deployed to `specs.bspoke.studio`. Each prospect gets a folder (e.g., `/torres-plumbing/index.html`).

## Structure
```
/[prospect-slug]/
  index.html
  styles.css (optional override)
  assets/ (optional)
```
